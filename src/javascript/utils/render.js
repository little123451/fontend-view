// 对接口返回的数据进行转换或者预处理，使其变成方便前端展示的数据
// 或者对前端表格数据进行转换，使其符合后端接口需要的数据

export default {
    /**
     * 默认方法
     * @param data
     * @returns {*}
     * @private
     */
    _default(data) {
        return data
    }
}