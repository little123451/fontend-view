doctype html
html
    head
        meta(charset="UTF-8")
        base(href="/")
        title Hello Vue
    body
        #root
    div#app {{message}}
    hr
    div#app-2
        span(v-bind:title="message") 鼠标悬停几秒钟查看此处动态绑定的提示信息！
    hr
    div#app-3
        p(v-if="seen") 现在你看到我了
        button(v-if="seen" v-on:click="hide") 消失吧
    hr
    div#app-4
        ol
            li(v-for="todo in todos") {{todo.text}}
    hr
    div#app-5
        p {{message}}
        button(v-on:click="reverseMessage") 逆转消息
    hr
    div#app-6
        p {{message}}
        input(v-model="message")
    hr
    div#app-7
        ol
            //  现在我们为每个 todo-item 提供 todo 对象
                todo 对象是变量，即其内容可以是动态的。
                我们也需要为每个组件提供一个“key”，稍后再
                作详细解释。
            todo-item(
                v-for="item in groceryList"
                v-bind:todo="item"
                v-bind:key="item.id"
            )
    hr
    div#app-8
        img(v-bind:src="src" width=100)
        button(v-on:click="setUpUrl") 加载
        button(v-if="src!=''" v-on:click="setOffUrl") 隐藏
    hr
    div#app-9
        button(v-on:click="change") 改变 Message
        p Message:{{msg}}
        p(v-once) 这个Message将不会改变: {{msg}}
        p Using mustaches: {{rawHtml}}
        p Using v-html directive:
            span(v-html="rawHtml")
    hr
    div#app-10
        p 事件冒泡 ( event.stop )
        div(v-on:click="divAClick" style="border:1px solid #000; padding:10px")
            p {{div_a}}
            button(v-on:click="btnAClick") {{btn_a}}
        br
        div(v-on:click="divBClick" style="border:1px solid #000; padding:10px")
            p {{div_b}}
            button(v-on:click.stop="btnBClick") {{btn_b}}
        p 阻止默认表现 ( event.prevent )
            p
                a(v-on:click.prevent="aClick" href="http://www.baidu.com") {{msg}}
    hr

    script(src="./javascript/vue.js")